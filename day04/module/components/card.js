export const makeCard = (imgSrc, title) => {
  article.appendChild(span);
  return article;
};
export const makeCard1 = (imgSrc, title) => {
  return;
  <article>
    <img src="" alt="" />
    <span></span>
  </article>;
};
